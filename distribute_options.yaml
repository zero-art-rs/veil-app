output: dist/
releases:
  - name: veil
    jobs:
      - name: linux-deb
        package:
          platform: linux
          target: deb
      - name: macos-dmg
        package:
          platform: macos
          target: dmg