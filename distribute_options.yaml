output: dist/
releases:
  - name: veil
    jobs:
      - name: linux-deb
        package:
          platform: linux
          target: deb
      - name: windows-veil
        package:
          platform: windows
          target: exe   