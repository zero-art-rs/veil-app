const ownerRole = 2;
const editorRole = 1;

const accountsTable = 'accounts';
const documentsTable = 'documents';
const documentMembersTable = 'document_members';

enum AccountKind { contact, user, familiar }
